bundle agent autorun_lsb
{
  meta:
      "tags" slist => { "autorun" };

  packages:
    (redhat_6|centos_6).enable_autorun_lsb::
      "redhat-lsb"
        policy => "present",
        package_module => yum;
}
